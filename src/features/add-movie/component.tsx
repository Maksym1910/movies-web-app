import React from 'react';

import styles from './component.module.scss';

const AddMovie = () => (
  <button type="button" className={styles.button}>+ ADD MOVIE</button>
);

export default AddMovie;
